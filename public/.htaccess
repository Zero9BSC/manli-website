# MANLI S.A.S. — Apache (mínimo para evitar 500 en hostings restrictivos)

# Servir index.html cuando se pide la raíz o un directorio
DirectoryIndex index.html

# Tipos MIME (solo si el servidor lo permite; si da 500, comenta o borra este bloque)
<IfModule mod_mime.c>
  AddType application/manifest+json .webmanifest
</IfModule>
